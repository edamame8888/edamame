１、６のnジョウ、９のnジョウはすべて異なる

その時の１０万を超えないコインの種類は２０種類ない

１０００００　＊　２０　は配列で取れるかつ時間的にOK

DPテーブルを使って実装する
coin[20]
dp[何種類のコインを使ったか][何円] = 何枚のコインを使うか
j = 10
if(j - coin[i] >= 0) dp[i][j] = min(dp[i][j] , dp[i][j - coin[i]] + 1);
